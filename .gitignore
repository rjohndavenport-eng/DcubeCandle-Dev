# Claude Code internal files
.claude/

# Loose scratch images at project root (not theme assets)
e4ec5e3c-cffa-4197-b974-c5200c81297d.png
09a67748-4f01-477a-b839-73e695277bb8.png
90d902bf-d848-4ba3-984a-76fd7fa5fd04.png

# OS / editor artifacts
.DS_Store
Thumbs.db
*.swp
*.swo

# Environment and secrets — NEVER commit these
.env
.env.*
.env.local
*.secret
secrets.*

# Shopify CLI state cache — generated, not source of truth
.shopify/
*.shopifyignore.cache

# Node / build artifacts
node_modules/
dist/
.cache/

# Diagnostic / transient pull directories — too large/noisy for git
dcube-sandbox-catalog/diagnostics/remote-pull/
dcube-sandbox-catalog/diagnostics/remote-full/
dcube-sandbox-catalog/diagnostics/post-fix-full/
dcube-sandbox-catalog/diagnostics/post-fix2-full/
dcube-sandbox-catalog/diagnostics/fix-push/
dcube-sandbox-catalog/diagnostics/integrity-check/
dcube-sandbox-catalog/diagnostics/final-verify/
dcube-sandbox-catalog/diagnostics/sync_log.txt

# Legacy theme download dir (replaced by shopify/themes/sandbox-160399261916/)
# NOTE: keep this tracked until migration confirmed complete
# dcube-sandbox-catalog/theme-download/

# Diagnostic snapshots and transient theme pulls
dcube-sandbox-catalog/diagnostics/smoke-verify/
dcube-sandbox-catalog/diagnostics/current_tree.txt

# Generated image output — large, not source of truth
dcube-sandbox-catalog/image-export/
